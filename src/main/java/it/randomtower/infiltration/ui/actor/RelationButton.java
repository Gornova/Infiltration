package it.randomtower.infiltration.ui.actor;

import java.awt.image.BufferedImage;

import it.randomtower.infiltration.ui.ImageButton;

@SuppressWarnings("serial")
public class RelationButton extends ImageButton {

	public RelationButton(BufferedImage image, BufferedImage onOverImage, String tooltip, int x, int y, int w, int h) {
		super(image, onOverImage, tooltip, x, y, w, h);
	}

}
